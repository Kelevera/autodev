# Autodev — The Self-Improving Codebase

A codebase that maintains itself: scans Python source for quality issues, plans
improvements, uses an LLM to generate fixes on isolated git branches, runs the
test suite, and commits only what passes.

> Full documentation lands in Phase 7. See `docs/ARCHITECTURE.md` meanwhile.
